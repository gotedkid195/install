Install pipenv for Ubunto 18.04

sudo apt install python3-pip python3-dev

pip3 install --user pipenv

echo "PATH=$HOME/.local/bin:$PATH" >> ~/.bashrc

source ~/.bashrc

Install pipenv for Ubunto 16.04
https://github.com/pypa/pipenv
